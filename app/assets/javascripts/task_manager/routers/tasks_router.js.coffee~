class Crm.Routers.Tasks extends Backbone.Router
  routes:
    '': 'index'
    
  initialize: ->
    @collection = new Crm.Collections.Tasks()
    @collection.fetch()
    
  index: ->
    view = new Crm.Views.TasksIndex(collection: @collection)
    $('#container').html(view.render().el)